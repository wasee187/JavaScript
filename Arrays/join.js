const numbers = [1,2,3,4];

const jointed = numbers.join(',');
console.log(jointed);


const message = 'This is my first message';

const parts = message.split(' ');
console.log(parts);
const combined = parts.join('-');
console.log(combined);

//mainly very applicable for URL 